import { MydirectriveDirective } from './mydirectrive.directive';

describe('MydirectriveDirective', () => {
  it('should create an instance', () => {
    const directive = new MydirectriveDirective();
    expect(directive).toBeTruthy();
  });
});
